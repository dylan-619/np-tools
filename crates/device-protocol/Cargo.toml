[package]
name = "device-protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
app-types = { path = "../app-types" }
serde = { version = "1.0", features = ["derive"] }
regex = "1.11"
thiserror = "2.0"
