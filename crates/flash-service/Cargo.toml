[package]
name = "flash-service"
version = "0.1.0"
edition = "2024"

[dependencies]
app-types = { path = "../app-types" }
tokio = { version = "1.41", features = ["full"] }
regex = "1.11"
serde = { version = "1.0", features = ["derive"] }
which = "7.0"
